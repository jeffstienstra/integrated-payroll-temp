---
type Props = {
	title: string;
	subtitle?: string;
};

const { title, subtitle } = Astro.props;
---

<section class="hero-panel">
	<div>
		<h1>{title}</h1>
		{subtitle && <p>{subtitle}</p>}
	</div>
</section>